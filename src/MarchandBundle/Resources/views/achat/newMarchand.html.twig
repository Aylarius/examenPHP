{% extends 'base.html.twig' %}

{% block body %}
    <h1>Nouvelle commande</h1>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    <input type="submit" value="Valider" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('achat_index_marchand') }}" class="btn btn-default mybtn">Retour à la liste des commandes</a>
        </li>
    </ul>
{% endblock %}
