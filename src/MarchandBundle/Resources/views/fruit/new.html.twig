{% extends 'base.html.twig' %}

{% block body %}
    <h1>Nouveau fruit</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Ajouter" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('achat_index_marchand') }}">Retour à l'inventaire</a>
        </li>
    </ul>
{% endblock %}
